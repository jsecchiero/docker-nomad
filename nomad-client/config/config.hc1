bind_addr = "0.0.0.0"

# Increase log verbosity
log_level = "INFO"

# Setup data dir
data_dir = "/data"

# Enable the server
client {
    enabled = true

    servers = ["172.16.110.1:4647"]
    options {
        "driver.raw_exec.enable" = "1"
    }
}
