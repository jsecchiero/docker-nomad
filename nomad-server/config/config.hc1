bind_addr = "0.0.0.0"

# Increase log verbosity
log_level = "INFO"

# Setup data dir
data_dir = "/data"

# Enable the server
server {
    enabled = true

    bootstrap_expect = 1
}

client {
    enabled = false
}
