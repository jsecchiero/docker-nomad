bind_addr = "nomad.home"

# Increase log verbosity
log_level = "DEBUG"

# Setup data dir
data_dir = "/data"

# Enable the server
server {
    enabled = true

    bootstrap_expect = 1
}

client {
    enabled = false
}
